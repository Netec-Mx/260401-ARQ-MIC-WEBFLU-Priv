server:
  port: 8081
course:
  environment: default
jwt:
  secret: ${JWT_SECRET:01234567890123456789012345678901}
management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      probes:
        enabled: true
